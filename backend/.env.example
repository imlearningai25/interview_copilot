# Backend-only env vars (subset of root .env.example)
# When using docker-compose, these are injected automatically.
# For running the backend standalone: copy to backend/.env

DATABASE_URL=postgresql://copilot:copilot_dev@localhost:4000/copilot_db
CORS_ORIGINS=http://localhost:4002,http://localhost:80
ENVIRONMENT=development
ANTHROPIC_API_KEY=sk-ant-...
